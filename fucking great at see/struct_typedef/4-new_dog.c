#include <stdio.h>
#include "dog.h"
dog_t *new_dog(char *name, float age, char *owner)
{
    scanf("%c", &name);
    scanf("%c", owner);
    if (new_dog == NULL)
        {
            return NULL;
        }
}
